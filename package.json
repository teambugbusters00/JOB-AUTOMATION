{
  "name": "job-automation-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android shell for JOB-AUTOMATION",
  "scripts": {
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync android",
    "android:build": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "@capacitor/android": "^7.4.3",
    "typescript": "^5.9.2"
  }
}
