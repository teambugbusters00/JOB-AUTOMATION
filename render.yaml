services:
  - type: web
    name: job-automation
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: sh docker/start.sh
    healthCheckPath: /health
    autoDeploy: true
