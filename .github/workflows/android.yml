name: Android App

on:
  workflow_dispatch:
    inputs:
      server_url:
        description: 'Render URL used by the Android app'
        required: false
        default: 'https://job-automation-8nm3.onrender.com'
        type: string
  push:
    branches: [main]
    paths:
      - 'web/**'
      - 'assets/**'
      - 'capacitor.config.ts'
      - 'package.json'
      - '.github/workflows/android.yml'

env:
  CAPACITOR_SERVER_URL: ${{ inputs.server_url || 'https://job-automation-8nm3.onrender.com' }}

jobs:
  build-android:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Install Capacitor and asset tools
        run: npm install

      - name: Generate Android project
        run: npx cap add android

      - name: Generate app icon from JOB-AUTOMATION logo
        run: npx @capacitor/assets generate --android --iconBackgroundColor '#020817' --iconBackgroundColorDark '#020817'

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Make Gradle executable
        run: chmod +x android/gradlew

      - name: Build installable debug APK
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: JOB-AUTOMATION-Android-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14

      - name: Copy APK into repository
        run: |
          mkdir -p apk
          cp android/app/build/outputs/apk/debug/app-debug.apk apk/JOB-AUTOMATION.apk

      - name: Commit APK to main
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -f apk/JOB-AUTOMATION.apk
          if git diff --cached --quiet; then
            echo 'APK unchanged; nothing to commit.'
          else
            git commit -m 'build: update Android APK'
            git push origin HEAD:main
          fi
